import {
  registerToken,
  registerUserEmail,
  registerAuth,
  registerUserId,
} from "./actions";
import userReducer from "./reducers";

export { registerToken, registerUserEmail, registerAuth, registerUserId };
export default userReducer;
